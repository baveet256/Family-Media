{
  "name": "family-media",
  "private": true,
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "api:dev": "npm run start:dev --prefix api",
    "api:build": "npm run build --prefix api",
    "api:lint": "npm run lint --prefix api",
    "api:test": "npm run test --prefix api",
    "mobile:dev": "npm run start --prefix mobile",
    "db:migrate": "npm run prisma:migrate --prefix api",
    "db:deploy": "npm run prisma:migrate:deploy --prefix api"
  }
}
